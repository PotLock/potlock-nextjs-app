import { METAPOOL_LIQUID_STAKING_CONTRACT_ACCOUNT_ID } from "@/common/_config";

export const POT_APPLICATION_REQUIREMENTS_MPDAO = {
  staking: {
    minAmountUsd: 25,
    tokenId: METAPOOL_LIQUID_STAKING_CONTRACT_ACCOUNT_ID,
    platformName: "Meta Pool",
  },
};
